import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
	appId: "io.github.jens_3.socialdeductiongame",
	appName: "Social Deduction Game",
	webDir: "dist",
};

export default config;
